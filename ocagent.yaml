
receivers:
  opencensus:
    address: ":55678"

exporters:
  jaeger:
    # The 'jaeger' service
    collector_endpoint: "jaeger:14268/api/traces"
  prometheus:
    # This host
    address: ":9100"
  zipkin:
    # The 'zipkin' service
    endpoint: "zipkin:9411/api/v2/spans"

zpages:
    port: 9999
